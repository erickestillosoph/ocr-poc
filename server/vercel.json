{
  "version": 2,
  "buildCommand": "rm -rf src/tmp/uploads/* && npm run build",
  "builds": [
    {
      "src": "src/api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/api/index.ts"
    }
  ]
}
